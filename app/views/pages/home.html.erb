
<div id="myCarousel" class="carousel slide" data-ride="carousel">
  <!-- Wrapper for slides -->
  <div class="carousel-inner" role="listbox">
    <div class="item active">
      <img src="/images/sitting.jpg" alt="dogsitters" />
      <div class="carousel-caption">
        <h4>Find the perfect sitter to stay with your furry friend while you're away.</h4>
      </div>
    </div>

    <div class="item">
      <img src="/images/training.jpg" alt="obedience trainers" />
      <div class="carousel-caption">
        <h4>Join today to browse through our database of trusted canine obedience trainers.</h4>
      </div>
    </div>

    <div class="item">
      <img src="/images/walking.jpg" alt="dog walkers" />
      <div class="carousel-caption">
        <h4>A new dogwalker for your pal is just a few clicks away.</h4>
      </div>
    </div>
  </div>

  <!-- Left and right controls -->
  <a class="left carousel-control" href="#myCarousel" role="button" data-slide="prev">
    <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
    <span class="sr-only">Previous</span>
  </a>
  <a class="right carousel-control" href="#myCarousel" role="button" data-slide="next">
    <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
    <span class="sr-only">Next</span>
  </a>
</div>
<div class="row">
  <% if user_signed_in? %>
    <div class="col-md-6">
      <div class="wellhead">
          <h2 class="text-center">Your Profile</h2>
        </div>

      <div class="well" id="signed-in">
        <%if current_user.plan_id == 2 %>
          <% if current_user.profile %>
            <%= link_to "Edit your profile", edit_user_profile_path(user_id: current_user.id), class: 'btn btn-default btn-lg btn-block' %>
            <%= link_to "View your profile", user_path(current_user), class: 'btn btn-default btn-lg btn-block' %>
          <% else %>
            <p>Create your profile so that you can share your information with the community.</p>
            <%= link_to "Create your profile", new_user_profile_path(current_user), class: 'btn btn-default btn-lg btn-block'%>
          <% end %><% end %>
        <% if current_user.plan_id == 1 %>
          <p>You have a basic account, which is limited. In order to make a profile and gain access to all our users' contact information, you need to sign up instead for a full account.</p>
          <p>Full accounts cost only $3/month to use!</p>
          <%end%>
        
      </div>
    </div>
    <div class="col-md-6">
      <div class="wellhead">
          <h2 class="text-center">The Community</h2>
        </div>
      <div class="well" id="signed-in">
        <p>Visit The Community to see a full directory of canine professionals in your area!</p>
        <%= link_to "Visit the community", users_path, class: 'btn btn-default btn-lg btn-block' %>
      </div>
    </div>
  <% else %>
    <div class="col-sm-6">
      <div class="wellhead">
        <h2 class="text-center">Basic Membership</h2>
      </div>
      <div class="well" id="signed-out">
        <p>Sign up for free and get a sneak-peek at our community's local Dogparc users.</p>
        <%= link_to "Sign up with Basic", new_user_registration_path(plan: @basic_plan.id), class: 'btn btn-default btn-lg btn-block' %>
      </div>
    </div>
    <div class="col-sm-6">
      <div class="wellhead">
        <h2 class="text-center">Full Membership</h2>
      </div>
      <div class="well" id="signed-out">
        <p>Sign up for the Full Account for $3/month and get access to users' contact info!</p>
        <%= link_to "Sign up with Full", new_user_registration_path(plan: @full_plan.id), class: 'btn btn-default btn-lg btn-block' %>
      </div>
    </div>
  <% end %>
</div>
</div>
